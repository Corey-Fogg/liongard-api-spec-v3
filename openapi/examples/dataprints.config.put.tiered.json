{
  "metadata": {
    "dataStructure": "tiered",
    "environments": {
      "arrayPath": "$.clients[*]",
      "namePath": "$.name",
      "idPath": "$.id"
    },
    "assets": {
      "arrayPath": "$.devices[*]",
      "namePath": "$.hostname",
      "typePath": "$.type",
      "uniqueIdPath": "$.serial_number"
    },
    "views": [
      {
        "type": "server",
        "fields": [
          "hostname",
          "type",
          "ip_address",
          "os"
        ]
      }
    ]
  }
}
